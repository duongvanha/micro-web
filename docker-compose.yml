version: '3'
services:

  nginx:
    restart: always
    build: nginx
    ports:
      - 4200:80

  postgres:
    env_file:
      - server/.env
    image: postgres:alpine

  redis:
    env_file:
      - server/.env
    image: redis:alpine

  client:
    build:
      context: client
      dockerfile: Dockerfile.${NODE_ENV}
    volumes:
      - ./client:/app

  server:
    env_file:
      - server/.env
    build:
      context: server
      dockerfile: Dockerfile.${NODE_ENV}
    volumes:
      - ./server:/app
    links:
      - postgres
      - redis

  worker:
    build: worker
    links:
      - postgres
      - redis