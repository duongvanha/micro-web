upstream client {
    server client:8080;
}

upstream server {
    server server:3000;
}


server {
    listen       80;


    location /api {
        rewrite /api(.*) /$1 break;
        proxy_pass http://server;
    }


}